CREATE DATABASE roger_partidaco;

USE roger_partidaco;

CREATE TABLE objetos_do_roger (
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    qtd INT,
    preco VARCHAR(100),
    situacao VARCHAR(50),
    imagem VARCHAR(500)
);

SELECT * FROM objetos_do_roger;

INSERT INTO objetos_do_roger (nome, qtd, preco, situacao, imagem)
VALUES ('chave_de_fenda', 78, 'R$67,00', 'disponivel', 'nada');

INSERT INTO objetos_do_roger (nome, qtd, preco, situacao, imagem)
VALUES ('alicate', 250, 'R$40,00', 'disponivel', 'nada');

INSERT INTO objetos_do_roger (nome, qtd, preco, situacao, imagem)
VALUES ('parafusadeira', 50, 'R$259,00', 'disponivel', 'nada');

INSERT INTO objetos_do_roger (nome, qtd, preco, situacao, imagem)
VALUES ('martelo', 500, 'R$89,00', 'disponivel', 'nada');

CREATE TABLE usuarios (
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    senha INT
);

SELECT * FROM usuarios;

INSERT INTO usuarios (nome, senha)
VALUES ('user', '1234');

INSERT INTO usuarios (nome, senha)
VALUES ('admin', '4321')